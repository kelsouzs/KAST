# Deep Learning Framework
tensorflow
torch
deepchem
pytorch-lightning  # Training framework for PyTorch (suppresses DeepChem warnings)
jax  # Google's JAX for GPU/TPU acceleration (suppresses DeepChem warnings)

# Data Processing
pandas
numpy
scikit-learn

# Visualization
matplotlib

# Progress Bars
tqdm

# Model Serialization
joblib

# Chemistry (requires conda - see installation instructions below)
# RDKit cannot be installed via pip. Use: conda install -c conda-forge rdkit
# rdkit